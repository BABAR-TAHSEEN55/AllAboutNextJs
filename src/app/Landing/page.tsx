import Navbar from "../../components/Navbar.tsx";

const page = () => {
    return (
        <div>
            <Navbar />
            <div>Initial page</div>
        </div>
    );
};

export default page;
