import type { Metadata } from "next";
export const metadata: Metadata = {
    title: "About Profile Page",
    description: "About Profile Page",
};
const page = () => {
    return <div>Profile Page</div>;
};

export default page;
